<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/home"
        android:title="@string/home"
        android:icon="@drawable/home_icon"
        app:showAsAction="always"
        />

    <item android:id="@+id/switch_profile"
        android:title="@string/switch_profile"
        android:icon="@drawable/user"
        app:showAsAction="always"/>

    <item android:id="@+id/world_entries"
        android:title="@string/world_entries"
        android:icon="@drawable/world_entries_icon"
        app:showAsAction="always"/>

    <item android:id="@+id/profile"
        android:title="@string/profile"
        android:icon="@drawable/user_icon_circle"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/setting"
        android:title="@string/setting"
        android:icon="@drawable/settings"
        app:showAsAction="always"/>

</menu>